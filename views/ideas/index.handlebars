{{#each ideas}}
  <div class="card card-body mb-2">
    <h4>{{title}}</h4>
    <p>{{details}}</p>
    <a class="btn btn-dark btn-block"href="/ideas/edit/{{_id}}">Edit</a>
  </div>
{{else}}
  <p>No study ideas listed</p>
{{/each}}